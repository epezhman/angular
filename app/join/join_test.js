'use strict';

describe('chatApp.join module', function() {

  beforeEach(module('chatApp.join'));

  describe('join controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var joinCtrl = $controller('JoinCtrl');
      expect(joinCtrl).toBeDefined();
    }));

  });
});