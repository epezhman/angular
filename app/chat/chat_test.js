'use strict';

describe('chatApp.chat module', function() {

  beforeEach(module('chatApp.chat'));

  describe('chat controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var chatCtrl = $controller('ChatCtrl');
      expect(chatCtrl).toBeDefined();
    }));

  });
});